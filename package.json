{
  "name": "viber-command-runner",
  "displayName": "Viber Command Runner",
  "description": "Command panel for VS Code: run workspace scripts with built-in terminal logs, run history, and pluggable remote sync.",
  "version": "0.3.0",
  "publisher": "nojsja",
  "license": "MIT",
  "icon": "media/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/nojsja/viber-command-runner.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "viberCommandRunner",
          "title": "Command Runner",
          "icon": "media/terminal.svg"
        }
      ]
    },
    "views": {
      "viberCommandRunner": [
        {
          "type": "webview",
          "id": "viberCommandRunner.launcher",
          "name": "Launcher"
        }
      ]
    },
    "commands": [
      {
        "command": "viberCommandRunner.openPanel",
        "title": "Viber Command Runner: 打开命令面板",
        "icon": "$(rocket)",
        "category": "Viber Command Runner"
      },
      {
        "command": "viberCommandRunner.refresh",
        "title": "Viber Command Runner: 刷新命令面板",
        "icon": "$(refresh)",
        "category": "Viber Command Runner"
      },
      {
        "command": "viberCommandRunner.openSettings",
        "title": "Viber Command Runner: 打开扩展设置",
        "category": "Viber Command Runner"
      },
      {
        "command": "viberCommandRunner.syncRemote",
        "title": "Viber Command Runner: 同步执行记录",
        "icon": "$(cloud-upload)",
        "category": "Viber Command Runner"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "viberCommandRunner.openPanel",
          "when": "view == viberCommandRunner.launcher",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Viber Command Runner",
      "properties": {
        "viberCommandRunner.commandsSource": {
          "type": "string",
          "enum": [
            "command-runner",
            "viberCommandRunner"
          ],
          "default": "command-runner",
          "description": "命令来源：读取 workspace 的 command-runner.commands 或面板内管理的预设命令"
        },
        "viberCommandRunner.terminalName": {
          "type": "string",
          "default": "Viber Command Runner",
          "description": "执行脚本时使用的终端名称"
        },
        "viberCommandRunner.shellPath": {
          "type": "string",
          "default": "",
          "description": "Optional shell executable for embedded command runner (e.g. /bin/zsh). Leave empty to auto-detect from $SHELL."
        },
        "viberCommandRunner.operator": {
          "type": "string",
          "default": "",
          "description": "操作人显示名，留空则自动读取 git config user.name / 系统用户名"
        },
        "viberCommandRunner.uiLanguage": {
          "type": "string",
          "enum": [
            "en",
            "zh"
          ],
          "default": "en",
          "enumDescriptions": [
            "English UI (default)",
            "中文界面"
          ],
          "description": "Command console UI language / 操作台界面语言"
        },
        "viberCommandRunner.silentNotifications": {
          "type": "boolean",
          "default": false,
          "description": "Suppress VS Code native notification sounds/toasts. Panel inline toasts are always shown. / 静默 VS Code 系统通知（含音效）；面板内提示仍会显示"
        }
      }
    }
  },
  "scripts": {
    "compile": "npm run compile:ext && npm run compile:webview",
    "compile:ext": "tsc -p ./",
    "compile:webview": "npm run build --prefix webview",
    "watch": "tsc -watch -p ./",
    "watch:webview": "npm run dev --prefix webview",
    "package": "npm run compile && vsce package",
    "lint": "tsc -p ./ --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.2.1",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "node-pty": "^1.1.0"
  }
}
